--extra-index-url https://download.pytorch.org/whl/cpu
flask>=2.3.0
psycopg2-binary>=2.9.0
waitress>=2.1.2
flask-cors>=4.0.0
pyjwt>=2.0.0
scikit-learn>=1.3.0
numpy>=1.26.0
# Load environment variables from .env
python-dotenv>=1.0.0
# Transformer stack
transformers>=4.46.0
sentence-transformers>=2.7.0
charset-normalizer>=3.2.0

# IMPORTANT: PyTorch wheels are often released after new Python versions. At the time
# of this repository update, Python 3.13 wheels for torch may not yet be available.
# Restrict to Python < 3.13 to avoid a partial install leading to:
# "cannot import name 'Tensor' from 'torch' (unknown location)".
# Use Python 3.12.x until official 3.13 wheels ship.

# Non-Windows or non-AMD64 platforms (generic CPU wheel)
torch>=2.4.1; python_version < '3.13' and (sys_platform != 'win32' or platform_machine != 'AMD64')
# Windows AMD64 explicit pin (helps ensure consistent wheel)
torch==2.4.1; python_version < '3.13' and sys_platform == 'win32' and platform_machine == 'AMD64'